<!DOCTYPE html>
<html xmlns='http://www.w3.org/1999/xhtml' lang='en'>
    <head>
        <meta charset='utf-8'/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
        <title>Moderation</title>
        <base href="/"></base>
        <link rel="stylesheet" href="/hissing-fauna/styles.css" media="all">
    </head>
    <body>
        <nav>
            <ul>
                <li><a{{#active_moderation}} class="active"{{/active_moderation}} href="/moderation">Moderation queue</a></li>
                <li><a{{#active_approved}} class="active"{{/active_approved}} href="/moderation/approved">Approved</a></li>
                <li><a{{#active_published}} class="active"{{/active_published}} href="/moderation/published">Published</a></li>
                <li><a{{#active_rejected}} class="active"{{/active_rejected}} href="/moderation/rejected">Rejected</a></li>
            </ul>
        </nav>
        <h1>Moderation</h1>
        <section id="entries">
            {{#posts}}
            <article rel="{{ timestamp }}">
                {{>moderation_actions}}
                <h2><span class="sAuteur">{{ author }}</span> - <span class="sType">{{ type }}</span></h2>
                {{#is_text}}
                <p class="sTexte">{{ text }}</p>
                {{/is_text}}
                {{#is_hybrid}}
                <p><span id="sTexte">{{ text }}</span><br><img class="sImage" src="/uploads/{{ image }}"></p>
                <p>W : {{ image_width }} -- H : {{ image_height }}</p>
                {{/is_hybrid}}
                {{#is_image}}
                <p><img class="sImage" src="/uploads/{{ image }}"></p>
                <p>W : {{ image_width }} -- H : {{ image_height }}</p>
                {{/is_image}}
            </article>
            {{/posts}}
        </section>
        <form id="formEntry">
            <ul>
                <li>
                    <label for="auteur">Auteur</label>
                    <input id="auteur" type="text" name="auteur" value="" />
                </li>
                <li>
                    <label for="texte">Texte</label>
                    <input id="texte" type="text" name="texte" value="">
                </li>
            </ul>
            <input id="btnCancel" type="button" value="Cancel" />
            <input id="btnSubmit" type="submit" value="Save" />
            <input id="formMode" type="hidden" value="" />
        </form>

        <script src="/hissing-fauna/jquery-1.8.0.min.js"></script>
        <script src="/hissing-fauna/scripts.js"></script>
    </body>
</html>